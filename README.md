# Learning AI

## Getting setup

```shell
mkdir learning_ai
cd learning_ai
touch README.md
git add .
git commit -m 'initial version'

gh repo create learning_ai --public --source . --remote=origin --push
```

